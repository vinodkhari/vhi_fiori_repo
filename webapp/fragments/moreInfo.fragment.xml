<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:form="sap.ui.layout.form">
    <form:SimpleForm>
        <form:content>
            <!-- <Label text="Fruit Name" />
            <Text text="{name}" />
            <Label text="Fruit Type" />
            <Text text="{type}" />
            <Label text="Price/Currency" />
            <Text text="{price} {currency}" />
            <Label text="Is Available" />
            <Text text="{status}" /> -->
            <!-- Odata Fragment changes labels -->
            <Label text="Product Name" />
            <Text text="{NAME}" />
            <Label text="Description" />
            <Text text="{DESCRIPTION}" />
            <Label text="Price/Currency" />
            <Text text="{PRICE} {CURRENCY_CODE}" />
            <Label text="Vendor Information" />
            <Text text="{SUPPLIER_ID} {SUPPLIER_NAME}" />
            <core:Title text="Vendor Details"></core:Title>
            <Label text="Email id:"></Label>
            <Text text="{To_Vendor/EMAIL_ADDRESS}"></Text>
            <Label text="City"></Label>
            <Text text="{To_Vendor/CITY}"></Text>
            <Label text="Country"></Label>
            <Text text="{To_Vendor/COUNTRY}"></Text>
        </form:content>
    </form:SimpleForm>
</core:FragmentDefinition>
